<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qxj.conclusion">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".mvp.view.WelcomeActivity"
            android:configChanges="orientation|keyboardHidden"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvp.view.LoginActivity"
            android:configChanges="orientation|keyboardHidden"
            android:launchMode="singleInstance" />
        <activity android:name=".mvp.view.MainActivity" />
        <activity
            android:name=".mvp.view.PermissionActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />
        <!--<activity android:name=".mvvm.view.MLoginActivity" />-->
        <activity
            android:name=".LauncherFlutterActivity"
            android:theme="@style/AppTheme.Flutter"></activity>
        <activity android:name=".dagger.FactoryActivity"></activity>
    </application>

</manifest>